#!/usr/bin/env python3
# TEMPUS — Corridors of Eternity :: LAN host launcher
#
#   ./tempus.run          serve the game to the LAN on port 6969
#
# Saves the world (saves/savegame.json) and every conversation with
# Minuette (saves/chatlog.jsonl). Requires only the Python 3 stdlib.

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from server.app import main

if __name__ == "__main__":
    main()
